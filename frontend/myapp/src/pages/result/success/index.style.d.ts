declare const useStyles: (props?: unknown) => import("antd-style").ReturnStyles<{
    title: {
        position: "relative";
        color: string;
        fontSize: string;
        textAlign: "center";
    };
    'head-title': {
        marginBottom: string;
        color: string;
        fontWeight: "500px";
        fontSize: string;
    };
}>;
export default useStyles;
