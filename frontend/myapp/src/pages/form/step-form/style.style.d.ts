declare const useStyles: (props?: unknown) => import("antd-style").ReturnStyles<{
    card: {
        marginBottom: string;
    };
    result: {
        maxWidth: string;
        margin: string;
        padding: string;
    };
}>;
export default useStyles;
