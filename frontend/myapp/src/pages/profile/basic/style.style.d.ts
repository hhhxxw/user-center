declare const useStyles: (props?: unknown) => import("antd-style").ReturnStyles<{
    title: {
        marginBottom: string;
        color: string;
        fontWeight: "500";
        fontSize: string;
    };
}>;
export default useStyles;
