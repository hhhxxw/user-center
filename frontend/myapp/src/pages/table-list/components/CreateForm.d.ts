import { ActionType } from '@ant-design/pro-components';
import { FC } from 'react';
interface CreateFormProps {
    reload?: ActionType['reload'];
}
declare const CreateForm: FC<CreateFormProps>;
export default CreateForm;
